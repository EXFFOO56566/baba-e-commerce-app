import { BarcodeScanerDirective } from './barcode-scaner.directive';

describe('BarcodeScanerDirective', () => {
  it('should create an instance', () => {
    const directive = new BarcodeScanerDirective();
    expect(directive).toBeTruthy();
  });
});
